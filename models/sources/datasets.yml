version: 2

sources:
  - name: retail
    tags: [kaggle]
    loader: SQLite CLI
    schema: dataset
    description: '{{ doc("scanner_data") }}'
    tables:
      - name: scanner_data
        description: >
          64.682 transactions of 5.242 SKU's sold to 22.625 customers during
          one year.
